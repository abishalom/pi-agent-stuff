# Experimental resources

This directory is for work-in-progress skills, prompts, and agents.

These files are committed to git but should stay out of the default package manifest until they are stable enough to ship.
